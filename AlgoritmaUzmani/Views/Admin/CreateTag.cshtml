@model AlgoritmaUzmani.Models.ViewModels.Admin.TagViewModel
@{
    ViewData["Title"] = "Yeni Etiket";
    Layout = "_AdminLayout";
}

<div class="card" style="max-width: 600px;">
    <div class="card-header">
        <h2 class="card-title">Yeni Etiket Oluştur</h2>
    </div>
    <div class="card-body">
        <form asp-action="CreateTag" method="post">
            @Html.AntiForgeryToken()

            <div class="form-group">
                <label asp-for="NameTr" class="form-label">İsim (TR) *</label>
                <input asp-for="NameTr" class="form-control" placeholder="Örn: Başlangıç" />
                <span asp-validation-for="NameTr" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="NameEn" class="form-label">İsim (EN)</label>
                <input asp-for="NameEn" class="form-control" placeholder="Örn: Beginner" />
            </div>

            <div class="form-group">
                <label asp-for="Color" class="form-label">Renk</label>
                <div style="display: flex; gap: 0.5rem; align-items: center;">
                    <input asp-for="Color" type="color" style="width: 50px; height: 38px; padding: 0; border: 1px solid var(--border); border-radius: 0.375rem; cursor: pointer;" />
                    <input type="text" id="colorText" class="form-control" placeholder="#3b82f6" style="flex: 1;" />
                </div>
                <div class="form-hint">Etikette gösterilecek arkaplan rengi</div>
            </div>

            <div class="form-group">
                <label class="form-label">Önizleme</label>
                <div>
                    <span id="tagPreview" class="badge" style="background: #dbeafe; color: #1e40af; font-size: 0.875rem; padding: 0.5rem 1rem;">
                        Etiket Önizleme
                    </span>
                </div>
            </div>

            <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i>
                    Kaydet
                </button>
                <a href="/admin/tags" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i>
                    İptal
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
<script>
    const colorInput = document.getElementById('Color');
    const colorText = document.getElementById('colorText');
    const preview = document.getElementById('tagPreview');
    const nameInput = document.getElementById('NameTr');

    colorInput.addEventListener('input', function() {
        colorText.value = this.value;
        preview.style.background = this.value;
        preview.style.color = '#fff';
    });

    colorText.addEventListener('input', function() {
        if (/^#[0-9A-Fa-f]{6}$/.test(this.value)) {
            colorInput.value = this.value;
            preview.style.background = this.value;
            preview.style.color = '#fff';
        }
    });

    nameInput.addEventListener('input', function() {
        preview.textContent = this.value || 'Etiket Önizleme';
    });
</script>
}

