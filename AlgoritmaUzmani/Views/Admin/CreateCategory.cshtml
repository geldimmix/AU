@model AlgoritmaUzmani.Models.ViewModels.Admin.CategoryViewModel
@{
    ViewData["Title"] = "Yeni Kategori";
    Layout = "_AdminLayout";
}

<div class="card" style="max-width: 800px;">
    <div class="card-header">
        <h2 class="card-title">Yeni Kategori Oluştur</h2>
    </div>
    <div class="card-body">
        <form asp-action="CreateCategory" method="post">
            @Html.AntiForgeryToken()

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="NameTr" class="form-label">İsim (TR) *</label>
                    <input asp-for="NameTr" class="form-control" placeholder="Örn: Veri Yapıları" />
                    <span asp-validation-for="NameTr" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="NameEn" class="form-label">İsim (EN)</label>
                    <input asp-for="NameEn" class="form-control" placeholder="Örn: Data Structures" />
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="DescriptionTr" class="form-label">Açıklama (TR)</label>
                    <textarea asp-for="DescriptionTr" class="form-control" rows="3" 
                              placeholder="Kategori hakkında kısa açıklama"></textarea>
                </div>
                <div class="form-group">
                    <label asp-for="DescriptionEn" class="form-label">Açıklama (EN)</label>
                    <textarea asp-for="DescriptionEn" class="form-control" rows="3" 
                              placeholder="Short description about category"></textarea>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="Icon" class="form-label">İkon (Emoji)</label>
                    <input asp-for="Icon" class="form-control" placeholder="Örn: 🗂️" />
                    <div class="form-hint">Windows: Win+. ile emoji seçin</div>
                </div>
                <div class="form-group">
                    <label asp-for="DisplayOrder" class="form-label">Sıralama</label>
                    <input asp-for="DisplayOrder" type="number" class="form-control" value="0" />
                </div>
            </div>

            <div class="form-group">
                <div class="form-check">
                    <input asp-for="IsActive" type="checkbox" checked />
                    <label asp-for="IsActive">Aktif</label>
                </div>
            </div>

            <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i>
                    Kaydet
                </button>
                <a href="/admin/categories" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i>
                    İptal
                </a>
            </div>
        </form>
    </div>
</div>

